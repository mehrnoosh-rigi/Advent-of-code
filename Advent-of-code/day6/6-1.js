var input = "14 0 15  12  11  11  3 5 1 6 8 4 9 1 8 4";
input = input.split(" ");
var array = []
console.log (typeof(input))
//var temp = Object.keys(input).map(function(key) {
  // return temp[key];
//});
var input = String(input);
array = [input];
console.log (typeof(array))

/*const calc_max = (acc, curr_value) =>{
  max = Math.max(acc, curr_value)
  index_max = index;
  return {
    max
  }
}

const max_inBlock = input.reduce(
  calc_max,
  input[0]
  );
*/
//console.log(max_inBlock);
console.log(typeof(input))